{
  "name": "mcp-discord-notify",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node_modules/.bin/esbuild index.mjs --bundle --platform=node --format=esm --outfile=dist/index.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
